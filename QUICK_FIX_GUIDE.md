# 🚨 Quick Fix Guide
# Folusho Victory Schools - Login Issue Resolution

## 🎯 Immediate Fix: Update Supabase Service

The issue is that the login function is checking `email` and `password` but not `role`. Let me fix this:

### **Step 1: Update Supabase Service**
<tool_call>edit
<arg_key>file_path</arg_key>
<arg_value>c:\Users\PASTOR\Desktop\folusho-victory-react\src\services\supabaseService.js
